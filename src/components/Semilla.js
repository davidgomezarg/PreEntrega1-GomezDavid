const Semilla = (props)=>{

    return(
        <div>
            <h3>{props.greeting}</h3>
            <h4>{props.tittlen}</h4>
            <p>{props.description}</p>
        </div>
    )
}

export default Semilla;